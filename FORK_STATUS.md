# JeopardyNesTextTool-fork — Status: Complete

This local fork is closed. Active development of the Jeopardy NES editor
continues in [`JeopardyClueWizard`](../JeopardyNesTextTool/JeopardyClueWizard),
which inherits this fork's UI experiments and adds the Phase 1-6 work
documented in `../JeopardyNesTextTool/FORK_ROADMAP.md`.

This directory is retained as a frozen reference for the cosmetic
group-budget panel (decimal byte counts with `bytes` units, percent /
slack collapsed into the State row, 120-px label column, three-column
per-block contribution table). Those changes have been ported into
ClueWizard's `View/MainWindow.xaml` and `ViewModel/ApplicationViewModel.cs`.

No further commits are expected here.
